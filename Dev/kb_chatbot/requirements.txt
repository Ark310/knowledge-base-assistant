PySide6>=6.6.0
sentence-transformers>=2.7
chromadb>=0.5
# Pinned range: the provider relies on the SDK's internal message-dict shape
# for multimodal streaming (verified against 0.2.87)
claude-agent-sdk>=0.2.87,<0.3
markdown>=3.6
rank-bm25>=0.2.2
pytest>=7.4.0
